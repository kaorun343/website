.page-header: h1 {{$root.current.lesson.title}}

ul.nav.nav-tabs.nav-justified
  li.active(role="presentaion"): a(sytle="pointer") 動画
  li(role="presentation"): a(style="pointer",
  v-on="click: $root.navigate('/lessons/'+$root.current.lesson.id+'/answer')"
  ) 設問（全{{$root.question_count}}問）
hr

div(v-youtube="{{$root.current.lesson.video_id}}")
